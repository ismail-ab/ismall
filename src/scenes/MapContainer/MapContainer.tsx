import * as React from 'react';
import { Map } from './components';

const MapContainer: React.SFC = () => (
  <>
    <h1>Select shops around you</h1>
    <Map />
  </>
);

export default MapContainer;